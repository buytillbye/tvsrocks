# Тестування 🧪

Стабільність — наш пріоритет. Проект містить розгалужену систему тестів для перевірки всіх рівнів додатку.

## Категорії тестів

### 1. Модульні тести (Unit Tests)
Перевірка окремих функцій, форматування, логіки часу та управління станом.
- **Розташування**: `tests/unit/`

### 2. Інтеграційні тести
Перевірка взаємодії між сервісами (напр. Orchestrator + Scanners).
- **Розташування**: `tests/integration/`

### 3. Стрес-тести (Chaos Testing)
Симуляція екстремальних умов: збої мережі, помилки API, швидкі переходи станів.
- **Ключовий файл**: `tests/ultimate_catalyst_chaos.js`

## Запуск тестів

### Запуск усіх тестів:
```bash
npm test
```

### Запуск конкретного стрес-тесту:
```bash
node tests/ultimate_catalyst_chaos.js
```

## Стратегія валідації
- Кожна нова функція повинна супроводжуватися відповідним тестом.
- Перед релізом обов'язково запускається повний цикл стрес-тестування.
- Логи тестів зберігаються в `tests/logs/` для подальшого аналізу.
