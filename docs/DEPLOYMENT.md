# Розгортання (Deployment) 🚀

Додаток підтримує кілька способів розгортання для забезпечення максимальної гнучкості.

## 1. Docker (Рекомендовано)
Ми використовуємо легкий образ на базі `alpine` для мінімізації розміру та підвищення безпеки.

### Збірка образу:
```bash
docker build -t screenstonks-watcher .
```

### Запуск контейнера:
```bash
docker run -d --env-file .env --name stocks-bot screenstonks-watcher
```

## 2. Fly.io
Проект містить конфігурацію `fly.toml` для швидкого розгортання в хмарі.

1. Встановіть `flyctl`.
2. Авторизуйтесь: `fly auth login`.
3. Створіть додаток: `fly launch`.
4. Налаштуйте секрети:
   ```bash
   fly secrets set BOT_TOKEN=your_token CHAT_ID=your_id ...
   ```
5. Розгорніть: `fly deploy`.

## 3. Локальний запуск (PM2)
Для роботи у фоновому режимі на власному сервері рекомендується використовувати PM2.

```bash
npm install pm2 -g
pm2 start src/index.js --name "stocks-bot"
```

---
> [!TIP]
> Переконайтеся, що на сервері встановлено Node.js версії 18 або вище та всі необхідні залежності для Playwright (якщо не використовується Docker).
