# Деплой на Fly.io (24/7 безкоштовно)

## 🚀 Швидкий старт

### 1. Підготовка

```bash
# Встанови Fly CLI
# Windows (PowerShell):
iwr https://fly.io/install.ps1 -useb | iex

# macOS/Linux:
curl -L https://fly.io/install.sh | sh
```

### 2. Реєстрація та логін

```bash
# Створи акаунт (безкоштовно)
fly auth signup

# Або залогінься
fly auth login
```

### 3. Ініціалізація проекту

```bash
# В папці проекту
fly launch

# Вибери:
# - App name: stocks10-watcher (або свою назву)
# - Region: fra (Frankfurt - близько до України)
# - Database: NO (не потрібна)
# - Deploy now: NO (спочатку налаштуємо змінні)
```

### 4. Налаштування змінних середовища

```bash
# Встанови свої змінні
fly secrets set BOT_TOKEN="твій_telegram_bot_token"
fly secrets set CHAT_ID="твій_chat_id"
fly secrets set THREAD_ID="твій_thread_id"  # опціонально
fly secrets set PREMARKET_THRESHOLD="10"
fly secrets set SCAN_INTERVAL_MS="10000"
fly secrets set SEND_ON_STARTUP="false"

# Перевір змінні
fly secrets list
```

### 5. Деплой

```bash
# Задеплой апку
fly deploy

# Перевір статус
fly status

# Подивись логи
fly logs
```

### 6. Моніторинг

```bash
# Живі логи
fly logs -f

# Статус машини
fly machine list

# SSH доступ (якщо потрібно)
fly ssh console
```

## 🔧 Налаштування fly.toml

Файл `fly.toml` вже створений з оптимальними налаштуваннями:

- **Регіон**: Frankfurt (близько до України)
- **Ресурси**: 256MB RAM, 1 CPU (достатньо)
- **Автозапуск**: Завжди 1 машина працює
- **Health check**: Перевірка кожні 30 секунд

## 💰 Безкоштовні ліміти

- **160GB-hours/місяць** (твоя апка ~0.25GB = 640 годин)
- **3GB трафіку/місяць**
- **Завжди активна** (не засинає)

## 🛠️ Корисні команди

```bash
# Перезапуск
fly machine restart

# Масштабування (якщо потрібно)
fly scale count 1

# Оновлення після змін коду
fly deploy

# Видалення (якщо потрібно)
fly apps destroy stocks10-watcher
```

## 🔍 Troubleshooting

### Апка не запускається:
```bash
fly logs
# Перевір змінні середовища та помилки
```

### Закінчились безкоштовні години:
```bash
fly billing show
# Додай картку для $5/місяць необмеженого використання
```

### Проблеми з Telegram:
- Перевір BOT_TOKEN та CHAT_ID
- Переконайся що бот доданий в чат

## 🎯 Альтернативи

Якщо Fly.io не підходить:

1. **Oracle Cloud Always Free** - назавжди безкоштовно
2. **Railway** - $5/місяць за необмежений час
3. **Google Cloud Run** - засинає між запитами
4. **Heroku** - засинає після 30 хв

## ✅ Готово!

Твоя апка тепер працює 24/7 безкоштовно! 🎉
